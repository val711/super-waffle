{
  "name": "Example Stripe iOS Backend",
  "description": "For testing the Stripe example iOS apps at github.com/stripe/stripe-ios",
  "repository": "https://github.com/val711/super-waffle/",
  "keywords": ["ios", "stripe"],
  "env": {
    "STRIPE_LIVE_SECRET_KEY": {
      "description": "Find this at https://dashboard.stripe.com/account/apikeys (it'll look like sk_test_****)",
      "required": true
    }
  }
}
